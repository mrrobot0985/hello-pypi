# info.py (in messages directory)
def handle_message(content):
    """
    Handles informational messages.
    """
    print(f"Info: {content}")  # This could also write to a dashboard or an external system
